version: '3.8'

services:
  moodle-downloader:
    build: .
    volumes:
      - ../Courses:/app/Courses
    environment:
      - MOODLE_USERNAME=${MOODLE_USERNAME}
      - MOODLE_PASSWORD=${MOODLE_PASSWORD}
      - MOODLE_URL=${MOODLE_URL}
